# Plates for `JourneyPaper`

Imported from the Claude Design project **"Nâng cấp thiết kế bài báo"**
(`56d6b87d-c2fc-4688-bb4a-1fbacce48336`), source document
`Journey to Independence v2 paper.dc.html`.

The design MCP caps a single file read at 256 KiB of base64 (~192 KB decoded),
so the files below came back **truncated** and are missing their tail. They
decode partially — `hero-portrait.png` still renders, `closing-crowd.png`
collapses to zero height. Re-download the project from claude.ai/design and
copy these over:

| file                   | status                        |
| ---------------------- | ----------------------------- |
| `nhandan-logo.png`     | **missing** — never written   |
| `closing-crowd.png`    | truncated                     |
| `hangngang-house.png`  | truncated                     |
| `hcm-planes.png`       | truncated                     |
| `hero-portrait.png`    | truncated                     |
| `moment-1945.png`      | truncated                     |
| `prison-scene.png`     | truncated                     |
| `tantrao-clearing.png` | truncated                     |
| `tantrao-parade.png`   | truncated                     |
| `ungho-crowd.png`      | truncated                     |

Complete and needing no action: `clipping.png`, `hcm-desk-teal.png`,
`hcm-newspaper.png`, `pacbo-rocks.png`, `soldiers-mist.png`,
`vietminh-flag.png`.
